#pragma once

enum class FaultType
{
    NONE,

    SENSOR_STUCK,
    SENSOR_OUT_OF_RANGE,
    SENSOR_IMPLAUSIBLE,

    PUMP_FAILURE,
    FAN_FAILURE,

    COOLING_INEFFECTIVE,

    THERMAL_IMBALANCE,

    WATCHDOG_TIMEOUT,

    OVER_TEMPERATURE,
    CELL_TEMP_IMBALANCE,
    MODULE_OVER_TEMP,
    PACK_OVER_TEMP,
    THERMAL_PROPAGATION_DETECTED,
    HOTSPOT_DETECTED
};
